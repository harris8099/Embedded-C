#include <stdio.h>
#include <stdint.h>

union Address{
	uint16_t shortAddr;
	uint32_t longAddr;
};

int main(void){
	union Address addr;

	addr.shortAddr = 0xabcd;
	addr.longAddr = 0xeeeecccc;

	printf("shortAddr = %x\n",addr.shortAddr);
	printf("longAddr = %x\n",addr.longAddr);

	return 0;
}
