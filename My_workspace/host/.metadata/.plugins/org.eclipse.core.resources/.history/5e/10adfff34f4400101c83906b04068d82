#include <stdio.h>
#include <stdint.h>

union Address{
	uint16_t shortAddr;
	uint32_t longAddr;
};

int main(void){
	union Address addr;

	addr.shortAddr = 0xabcd;
	addr.longAddr = 0xeeeecccc;

	printf("shortAddr = %x\n",shortAddr);
	printf("longAddr = %x\n",longAddr);

	return 0;
}
